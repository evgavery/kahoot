import { DestroySubDirective } from './destroy-sub.directive';

describe('DestroySubDirective', () => {
  it('should create an instance', () => {
    const directive = new DestroySubDirective();
    expect(directive).toBeTruthy();
  });
});
